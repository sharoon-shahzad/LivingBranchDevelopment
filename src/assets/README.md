## `src/assets`

Optional production-grade convention for assets imported via `astro:assets`.

This project currently keeps most images in `src/images/` for backwards compatibility.
If you migrate assets later, prefer:

- `src/assets/images/`
- `public/` for files that must be served at a fixed URL path

