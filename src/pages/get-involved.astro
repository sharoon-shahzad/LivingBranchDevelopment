---
import Layout from "../layouts/Layout.astro";

const programmeLinks = [
  {
    title: "Education",
    description: "Keep children learning with tutoring and family support.",
    href: "/our-work/education"
  },
  {
    title: "Health Care",
    description: "Bring screening, maternal care, and referrals closer to families.",
    href: "/our-work/health-care"
  },
  {
    title: "Skill Development",
    description: "Train youth and caregivers for steady, dignified livelihoods.",
    href: "/our-work/skill-development"
  },
  {
    title: "Women Empowerment",
    description: "Support safety, leadership, and economic resilience for women.",
    href: "/our-work/women-empowerment"
  }
];

const partnerReasons = [
  {
    title: "Community-led delivery",
    description: "We co-design every intervention with local leaders so support stays relevant and trusted."
  },
  {
    title: "Transparent reporting",
    description: "Clear updates, budgets, and milestones keep partners informed at every stage."
  },
  {
    title: "Measured outcomes",
    description: "We track progress with simple, human metrics that show real changes in daily life."
  }
];

const trustStats = [
  { value: "15k+", label: "People reached" },
  { value: "120+", label: "Communities" },
  { value: "260+", label: "Local volunteers" }
];
---

<Layout title="Get involved | Living Branch Development">
  <section class="get-involved-hero">
    <div class="container-page grid gap-10 py-16 lg:grid-cols-[1.15fr_0.85fr] lg:items-center">
      <div>
        <p
          class="inline-flex items-center gap-2 rounded-full bg-white/80 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[color-mix(in_srgb,var(--brand-navy)_80%,black)] shadow-sm ring-1 ring-black/5"
        >
          Get involved
        </p>
        <h1 class="mt-4 font-heading text-4xl font-semibold tracking-tight text-[var(--fg)] sm:text-5xl">
          Your time and partnership can rewrite a family’s future.
        </h1>
        <p class="mt-4 max-w-xl text-base leading-relaxed text-[var(--muted)] sm:text-lg">
          Every programme is built with communities, not just for them. Join us with resources, expertise, or
          funding that keeps essential support consistent.
        </p>
        <div class="mt-7 flex flex-wrap gap-3">
          <a
            href="/get-involved#partner"
            class="inline-flex items-center justify-center rounded-full bg-[var(--brand-navy)] px-6 py-3 text-sm font-semibold text-white shadow-sm transition hover:bg-[color-mix(in_srgb,var(--brand-navy)_85%,black)]"
          >
            Partner with us
          </a>
          <a
            href="/donate"
            class="inline-flex items-center justify-center rounded-full bg-[var(--brand-cyan)] px-6 py-3 text-sm font-semibold text-white shadow-sm transition hover:bg-[color-mix(in_srgb,var(--brand-cyan)_88%,black)]"
          >
            Donate now
          </a>
        </div>
      </div>

      <div class="hero-card">
        <div class="hero-icon-grid">
          <div class="hero-icon">
            <div class="hero-icon-badge" aria-hidden="true">
              <svg viewBox="0 0 24 24" class="h-6 w-6" aria-hidden="true">
                <path
                  fill="currentColor"
                  d="M8 10.5 6 8.6A3 3 0 1 1 10.2 4l1.8 2 1.9-2A3 3 0 1 1 18 8.6l-2 1.9v3.8a3.5 3.5 0 0 1-7 0v-3.8Z"
                />
              </svg>
            </div>
            <h3 class="font-heading text-sm font-semibold text-[var(--fg)]">Donate</h3>
            <p class="text-xs text-[var(--muted)]">Fund essentials that families rely on.</p>
          </div>
          <div class="hero-icon">
            <div class="hero-icon-badge" aria-hidden="true">
              <svg viewBox="0 0 24 24" class="h-6 w-6" aria-hidden="true">
                <path
                  fill="currentColor"
                  d="M7.5 12a3.5 3.5 0 0 1 6.7-1.4l3.3 3.3a2.5 2.5 0 0 1-3.6 3.5l-3.1-3.2-2.1 2.1a2.5 2.5 0 0 1-3.6-3.5Z"
                />
              </svg>
            </div>
            <h3 class="font-heading text-sm font-semibold text-[var(--fg)]">Partner</h3>
            <p class="text-xs text-[var(--muted)]">Co-create solutions and scale impact.</p>
          </div>
          <div class="hero-icon">
            <div class="hero-icon-badge" aria-hidden="true">
              <svg viewBox="0 0 24 24" class="h-6 w-6" aria-hidden="true">
                <path
                  fill="currentColor"
                  d="M12 4a4 4 0 1 1 0 8 4 4 0 0 1 0-8Zm-6.5 15a4.5 4.5 0 0 1 9 0v1H5.5v-1Zm10 0a4.5 4.5 0 0 1 9 0v1h-9v-1Z"
                />
              </svg>
            </div>
            <h3 class="font-heading text-sm font-semibold text-[var(--fg)]">Volunteer</h3>
            <p class="text-xs text-[var(--muted)]">Give your time and skills locally.</p>
          </div>
          <div class="hero-icon">
            <div class="hero-icon-badge" aria-hidden="true">
              <svg viewBox="0 0 24 24" class="h-6 w-6" aria-hidden="true">
                <path
                  fill="currentColor"
                  d="M4 7.5A3.5 3.5 0 0 1 7.5 4h9A3.5 3.5 0 0 1 20 7.5v9a3.5 3.5 0 0 1-3.5 3.5h-9A3.5 3.5 0 0 1 4 16.5v-9Z"
                />
                <path fill="#fff" d="M8 9h8v2H8V9Zm0 4h5v2H8v-2Z" />
              </svg>
            </div>
            <h3 class="font-heading text-sm font-semibold text-[var(--fg)]">Mentor</h3>
            <p class="text-xs text-[var(--muted)]">Guide learners into new careers.</p>
          </div>
        </div>
        <div class="hero-note">
          <p class="text-xs uppercase tracking-[0.2em] text-[var(--muted)]">Where you come in</p>
          <p class="mt-2 text-sm text-[var(--fg)]">
            Pick a path below and we will connect you to the right programme team.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="container-page py-12 sm:py-14">
    <div class="programme-header">
      <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[color-mix(in_srgb,var(--brand-navy)_70%,var(--muted))]">
        Support a programme
      </p>
      <h2 class="mt-3 font-heading text-2xl font-semibold tracking-tight text-[var(--fg)] sm:text-3xl">
        Choose where you want your impact to begin.
      </h2>
      <p class="mt-3 max-w-2xl text-sm leading-relaxed text-[var(--muted)] sm:text-base">
        Each programme meets a different community need. Pick a focus area and we will guide you to the right team.
      </p>
    </div>
    <div class="programme-grid">
      {programmeLinks.map((programme) => (
        <a href={programme.href} class="programme-card">
          <div>
            <h3 class="font-heading text-lg font-semibold text-[var(--fg)]">{programme.title}</h3>
            <p class="mt-2 text-sm leading-relaxed text-[var(--muted)]">{programme.description}</p>
          </div>
          <span class="programme-link">Explore programme</span>
        </a>
      ))}
    </div>
  </section>

  <section class="partner-band" id="partner">
    <div class="container-page py-12 sm:py-14">
      <div class="partner-header">
        <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[color-mix(in_srgb,var(--brand-navy)_70%,var(--muted))]">
          Why partner with us
        </p>
        <h2 class="mt-3 font-heading text-2xl font-semibold tracking-tight text-[var(--fg)] sm:text-3xl">
          Trusted collaboration that turns support into outcomes.
        </h2>
        <p class="mt-3 max-w-2xl text-sm leading-relaxed text-[var(--muted)] sm:text-base">
          We build long-term partnerships rooted in local insight, shared accountability, and measurable impact.
        </p>
      </div>
      <div class="partner-grid">
        {partnerReasons.map((reason, index) => (
          <div class="partner-card">
            <span class="partner-index">0{index + 1}</span>
            <h3 class="mt-4 font-heading text-lg font-semibold text-[var(--fg)]">{reason.title}</h3>
            <p class="mt-2 text-sm leading-relaxed text-[var(--muted)]">{reason.description}</p>
          </div>
        ))}
      </div>
      <div class="partner-cta">
        <div>
          <h3 class="font-heading text-xl font-semibold text-[var(--fg)]">Let’s build a partnership plan.</h3>
          <p class="mt-2 text-sm text-[var(--muted)]">
            We will align your goals with the programmes that need you most.
          </p>
        </div>
        <a
          href="/contact"
          class="inline-flex items-center justify-center rounded-full bg-[var(--brand-navy)] px-5 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-[color-mix(in_srgb,var(--brand-navy)_85%,black)]"
        >
          Start a partnership
        </a>
      </div>
    </div>
  </section>

  <section class="trust-band">
    <div class="container-page py-12 sm:py-14">
      <div class="trust-grid">
        <div>
          <p class="text-xs font-semibold uppercase tracking-[0.2em] text-[color-mix(in_srgb,var(--brand-navy)_70%,var(--muted))]">
            Trust & impact
          </p>
          <h2 class="mt-3 font-heading text-2xl font-semibold tracking-tight text-[var(--fg)] sm:text-3xl">
            Built with communities, measured with care.
          </h2>
          <p class="mt-3 max-w-xl text-sm leading-relaxed text-[var(--muted)] sm:text-base">
            Partners stay with us because they see consistent delivery, honest reporting, and outcomes that last.
          </p>
          <div class="mt-6 grid gap-3 sm:grid-cols-3">
            {trustStats.map((stat) => (
              <div class="trust-stat">
                <p class="text-xl font-semibold text-[var(--fg)]">{stat.value}</p>
                <p class="mt-1 text-xs uppercase tracking-[0.2em] text-[var(--muted)]">{stat.label}</p>
              </div>
            ))}
          </div>
        </div>
        <div class="trust-quote">
          <p class="text-sm uppercase tracking-[0.2em] text-[var(--muted)]">Partner note</p>
          <p class="mt-4 text-lg leading-relaxed text-[var(--fg)]">
            “Living Branch Development makes it easy to align our CSR goals with real community needs. The
            collaboration feels honest and grounded.”
          </p>
          <p class="mt-4 text-sm font-semibold text-[color-mix(in_srgb,var(--brand-navy)_75%,black)]">
            Corner Stone Community Development, Pakistan
          </p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .get-involved-hero {
    background: #f6f8ff;
  }

  .hero-card {
    border-radius: 28px;
    border: 1px solid var(--border);
    background: #ffffff;
    padding: 1.8rem;
    box-shadow: 0 18px 40px rgba(12, 16, 34, 0.08);
    display: grid;
    gap: 1.4rem;
  }

  .hero-icon-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .hero-icon {
    border-radius: 18px;
    border: 1px solid var(--border);
    background: #f9f9ff;
    padding: 1rem;
    display: grid;
    gap: 0.4rem;
  }

  .hero-icon-badge {
    height: 40px;
    width: 40px;
    border-radius: 14px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: color-mix(in srgb, var(--brand-cyan) 18%, white);
    color: color-mix(in srgb, var(--brand-navy) 85%, black);
  }

  .hero-note {
    border-radius: 18px;
    border: 1px dashed color-mix(in srgb, var(--brand-navy) 20%, white);
    background: #f7f8ff;
    padding: 1rem 1.1rem;
  }

  .programme-header {
    max-width: 44rem;
  }

  .programme-grid {
    margin-top: 2rem;
    display: grid;
    gap: 1.2rem;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }

  .programme-card {
    border-radius: 22px;
    border: 1px solid var(--border);
    background: #ffffff;
    padding: 1.4rem;
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    justify-content: space-between;
    box-shadow: 0 12px 22px rgba(12, 16, 34, 0.08);
    transition: transform 160ms ease, box-shadow 160ms ease, border-color 160ms ease;
  }

  .programme-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 18px 30px rgba(12, 16, 34, 0.12);
    border-color: color-mix(in srgb, var(--brand-navy) 20%, var(--border));
  }

  .programme-link {
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--brand-navy) 80%, black);
  }

  .partner-band {
    background: #f8f5ff;
  }

  .partner-header {
    max-width: 44rem;
  }

  .partner-grid {
    margin-top: 2rem;
    display: grid;
    gap: 1.2rem;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }

  .partner-card {
    border-radius: 22px;
    border: 1px solid var(--border);
    background: #ffffff;
    padding: 1.4rem;
    box-shadow: 0 12px 22px rgba(12, 16, 34, 0.08);
  }

  .partner-index {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: color-mix(in srgb, var(--brand-navy) 12%, white);
    padding: 0.35rem 0.7rem;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: color-mix(in srgb, var(--brand-navy) 80%, black);
  }

  .partner-cta {
    margin-top: 2rem;
    border-radius: 22px;
    border: 1px solid var(--border);
    background: #ffffff;
    padding: 1.2rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    box-shadow: 0 12px 22px rgba(12, 16, 34, 0.08);
  }

  .trust-band {
    background: #f6fbff;
  }

  .trust-grid {
    display: grid;
    gap: 2rem;
    align-items: center;
  }

  .trust-stat {
    border-radius: 16px;
    border: 1px solid var(--border);
    background: #ffffff;
    padding: 0.9rem 1rem;
    box-shadow: 0 10px 18px rgba(12, 16, 34, 0.06);
  }

  .trust-quote {
    border-radius: 24px;
    border: 1px solid var(--border);
    background: #ffffff;
    padding: 1.6rem;
    box-shadow: 0 14px 26px rgba(12, 16, 34, 0.08);
  }

  @media (min-width: 768px) {
    .partner-cta {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }

  @media (min-width: 1024px) {
    .trust-grid {
      grid-template-columns: 1.05fr 0.95fr;
    }
  }

  @media (min-width: 1024px) {
    .hero-icon-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
</style>

