## `src/lib`

Production-grade convention for shared, framework-agnostic code:

- Helpers (formatting, dates, urls)
- Analytics wrappers
- Feature utilities used across pages/components

Keep this folder free of UI components.

